/*
 * Lancer la commande suivante pour insérer les données
 * cat cretab_app.sqlite | sqlite3 test_database.sdb
 */
 
CREATE TABLE app (
	id integer NOT NULL PRIMARY KEY AUTOINCREMENT , 
	name varchar(255) NOT NULL , 
	owner_id integer NOT NULL , 
	server varchar(255) 
);



